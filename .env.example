# ================================================================
# Configuration AWS - VPN WireGuard
# ================================================================
# IMPORTANT: Remplacez TOUTES les valeurs ci-dessous par vos vraies informations

# CLES D'ACCES AWS (OBLIGATOIRE - obtenez-les dans la console AWS IAM)
# Format: AKIAIOSFODNN7EXAMPLE (commence toujours par AKIA...)
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE

# Format: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY (40 caractères alphanumériques avec /)
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# REGION AWS (choisissez la plus proche de vous)
# Exemples: eu-west-3 (Paris), us-east-1 (Virginie), ap-southeast-1 (Singapour)
AWS_DEFAULT_REGION=eu-west-3

# ================================================================
# Configuration du projet (modifiable selon vos préférences)
# ================================================================

# NOM DU PROJET (utilisé pour nommer toutes les ressources AWS)
# Règles: lettres, chiffres, tirets uniquement. Max 32 caractères.
# Exemple: mon-vpn-perso, vpn-famille-martin, entreprise-vpn-dev
PROJECT_NAME=mon-vpn-personnel

# NOMS DES RESSOURCES AWS (préfixés automatiquement par PROJECT_NAME)
KEY_PAIR_NAME=wireguard-vpn-key
VPC_NAME=wireguard-vpc
SUBNET_NAME=wireguard-subnet
SECURITY_GROUP_NAME=wireguard-security-group
INTERNET_GATEWAY_NAME=wireguard-igw
ROUTE_TABLE_NAME=wireguard-rt

# ================================================================
# Configuration réseau VPN (recommandé de garder par défaut)
# ================================================================

# SOUS-RESEAU VPN interne (CIDR notation)
# Par défaut: 192.168.100.0/24 (clients auront IPs .2 à .254)
# Alternatives: 10.0.0.0/24, 172.16.0.0/24
VPN_SUBNET=192.168.100.0/24

# IP DU SERVEUR VPN (première IP du sous-réseau)
VPN_SERVER_IP=192.168.100.1

# PORT UDP pour WireGuard (standard: 51820)
# Alternative courante: 443 (pour contourner certains firewalls)
VPN_PORT=51820

# ================================================================
# Configuration instance EC2 (recommandé de garder par défaut)
# ================================================================

# TYPE D'INSTANCE (t2.micro est GRATUIT avec Free Tier)
# Ne changez QUE si vous avez dépassé le Free Tier
INSTANCE_TYPE=t2.micro

# NOM DE L'INSTANCE dans la console AWS
INSTANCE_NAME=wireguard-vpn-server

# ================================================================
# Tags AWS (optionnel - pour organisation/facturation)
# ================================================================

# ENVIRONNEMENT (pour différencier dev/test/prod)
ENVIRONMENT=production

# PROPRIETAIRE/responsable (votre nom ou équipe)
OWNER=votre-nom-ici

# ================================================================
# GUIDE RAPIDE:
# ================================================================
# 1. Obtenez vos clés AWS:
#    Console AWS → IAM → Utilisateurs → Créer utilisateur → Accès programmatique
#    Permissions nécessaires: EC2FullAccess, VPCFullAccess
#
# 2. Copiez ce fichier:
#    cp .env.example .env
#
# 3. Éditez .env avec vos vraies valeurs:
#    nano .env
#
# 4. Gardez .env SECRET (déjà protégé par .gitignore)
#
# 5. Lancez le déploiement:
#    ./deploy_vpn.sh
# ================================================================
